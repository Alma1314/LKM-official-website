---
import SectionWrapper from './SectionWrapper.astro';
import { t } from '~/lib/i18n';

export interface Props {
  title: string;
  intro?: string;
  id?: string;
}

const { title, intro, id } = Astro.props;
---

<SectionWrapper id={id}>
  <div class="max-w-7xl mx-auto">
    <h3 class="text-xl font-bold mb-2 text-center">
      <span class="border-b-2 border-primary pb-1">{t(title)}</span>
    </h3>
    {
      intro && (
        <p class="max-w-3xl mx-auto mb-6 px-6 py-4 bg-page-bg border border-surface-3 rounded-[8px] text-center text-[14px] text-text-muted leading-relaxed card-hover ">
          {t(intro)}
        </p>
      )
    }
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
      <slot />
    </div>
  </div>
</SectionWrapper>
