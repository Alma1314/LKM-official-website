---
import { t } from '~/lib/i18n';

interface Props {
  message?: string;
}

const { message = t('primitives.empty') } = Astro.props;
---

<div class="flex min-h-[200px] items-center justify-center text-muted-foreground">
  <p>{message}</p>
</div>
