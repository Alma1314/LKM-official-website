---
import Layout from '~/layouts/SidebarLayout.astro';
import { communitySidebarLinks } from '~/features/shell/data/community-sidebar';
import AskFormPage from '~/features/qa/components/AskFormPage.vue';
import { getPermalink } from '~/core/utils/permalinks';

const metadata = { title: '提出问题 —— 理科迷' };
---

<Layout metadata={metadata} sidebarLinks={communitySidebarLinks} currentPath={getPermalink('/qa/ask')}>
  <div style="min-height: 400px"><AskFormPage client:only="vue" /></div>
</Layout>
