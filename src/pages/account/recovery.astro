---
import PageLayout from '~/layouts/PageLayout.astro';
import RecoveryPage from '~/features/auth/components/recovery/RecoveryPage.vue';
import { t } from '~/lib/i18n';

const metadata = { title: t('page.misc.recoveryTitle') };
---

<PageLayout metadata={metadata}>
  <RecoveryPage client:only="vue" />
</PageLayout>
