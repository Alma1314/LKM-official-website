services:
  astro:
    build: .
    ports:
      - '80:4321'
    environment:
      - NODE_ENV=production
      # 真实后端地址，由部署环境注入（如 -e API_URL=https://api.lkm.app）
      - API_URL=${API_URL}
    restart: unless-stopped
